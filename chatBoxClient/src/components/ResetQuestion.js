function ResetQuestions() {
  // Need to have a option where can take the user back to starting question
  // User should be able to select Yes/No Back to the start
  // If No end the conversation
  // If Yes Go back to the start
  resetMain = () => {};
}

export default ResetQuestions;
